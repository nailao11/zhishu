# Paste your full session Cookie below (no quotes), then save.
# Open target site -> log in -> F12 -> Network -> any request ->
# Request Headers -> Cookie -> copy the whole line and put it here.
#
# (Do NOT leave only this header in production - the loader requires real content.)
